import { RestaltarDirective } from './restaltar.directive';

describe('RestaltarDirective', () => {
  it('should create an instance', () => {
    const directive = new RestaltarDirective();
    expect(directive).toBeTruthy();
  });
});
