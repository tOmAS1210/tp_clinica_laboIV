import { ReziceOnHoverDirective } from './rezice-on-hover.directive';

describe('ReziceOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ReziceOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
