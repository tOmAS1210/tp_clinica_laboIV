import { OcultarCaracteresPipe } from './ocultar-caracteres.pipe';

describe('OcultarCaracteresPipe', () => {
  it('create an instance', () => {
    const pipe = new OcultarCaracteresPipe();
    expect(pipe).toBeTruthy();
  });
});
